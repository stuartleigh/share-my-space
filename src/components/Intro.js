import React, { Component } from 'react';

export default class Header extends Component {
  render () {
    return (
      <section className="intro">
        <p>{"There are so many ways to use extra space. Rent it out on Airbnb, build a co-working space, or turn it into a pop-up shop. What makes the most sense? What generates the most income? Does it change depending on area? Basically, how should I Share My Space?"}</p>
      </section>
    );
  }
}
